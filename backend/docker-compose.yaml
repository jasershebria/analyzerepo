version: '3.8'

services:
  postgres:
    image: postgres
    container_name: analyzerepo-postgres
    restart: always
    environment:
      POSTGRES_DB: analyzerepo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"